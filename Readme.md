imageResize
图像大小的缩放
实现步骤：
	1、利用io实现指定文件夹下，相同类型的文件的遍历。
	2、利用opencv的resize（）函数实现图像大小的变化。
	3、重新写入新的文件夹下。
	注意：仅能在Windows下实现

需要设置的参数：
	通过宏定义修改参数
	1、目录的类别总数
	2、目录的路径，以\\结尾
	3、文件类型
	4、新文件的存放目录
	5、缩放后的图像大小。（可以按比例缩放）
	文件夹的设置如下所示：
	src\samples
	src\samples\resized